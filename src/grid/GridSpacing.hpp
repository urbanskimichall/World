#pragma once
#include <cstdint>

namespace grid
{
    constexpr float GRID_SPACING = 10.0f;
    constexpr float MIN_GRID_SPACING = 5.0f;
    constexpr float MAX_GRID_SPACING = 50.0f;

    constexpr std::uint32_t numOfRows = 30;
}